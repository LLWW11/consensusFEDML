# Codex 工作日志

## 2026-07-28：SevereTest 单标签极端非 IID 实验

### 实施内容

- 新建独立目录 `SevereTest/`，未修改根目录既有训练、MATLAB 拓扑和历史分析代码。
- 实现 MNIST 的 `label_modulo_20` 确定性划分：共 200 个客户端，每个客户端仅包含 `client_id % 10` 对应的一个标签，每类均分给 20 个客户端。
- 实现固定客户端 0–9 的同步 FedAvg：每轮从云模型开始本地训练 1 个 epoch，按训练样本数加权聚合，聚合后向客户端 0–199 显式下发。
- 实现固定 100 张探针及每轮本地概率记录，保存客户端、云端和空边缘层的统一 NPZ 接口。
- 实现从原始 NPZ 重算概率一致性 A、预测确定性 C、有效共识 S、正确有效共识和错误有效共识的分析程序，并生成总体趋势、准确率趋势和逐类准确率热力图。
- 新增正式配置、两轮冒烟配置、简体中文说明文档和单元测试；YAML 注释均使用英文。

### 测试与校验

- 单元测试命令：`conda run --no-capture-output -n py37 python -m unittest -v SevereTest.tests.test_severe_experiment`。
- 单元测试共 8 项，全部通过；覆盖确定性划分、无空客户端、单标签约束、样本无丢失或重复、固定训练名单、样本加权聚合和有效共识边界。
- 代码说明审计通过：`SevereTest/` 中没有缺少说明文档字符串的函数或类方法。
- 两轮 CPU 冒烟实验成功，结果目录为 `result/SevereTest/smoke_mnist_single_label_first10_fedavg_2round_20260728_192816/`。
- 正式 200 轮 CPU 实验成功，耗时约 1694.3 秒，结果目录为 `result/SevereTest/mnist_single_label_first10_fedavg_200round_20260728_193035/`。
- 正式结果包含 200 行总体测试指标、200 行探针摘要、200 行调度日志和 2000 行逐类指标。
- 探针概率形状为 `[200, 10, 100, 10]`，客户端编号严格为 0–9，活跃掩码全真；概率范围合法，单行概率和最大误差约为 `4.17e-7`。
- 所有 200 轮调度均为活跃客户端 0–9，并向客户端 0–199 下发。
- 分析程序已从 NPZ 逐轮重算 A、C、S，并与训练期摘要核对一致。

### 正式结果与分析

- 最后 20 轮完整测试准确率为 `0.86475 ± 0.01785`，最终轮为 `0.8102`，最好值为第 176 轮的 `0.8827`。
- 最后 20 轮有效共识 S 为 `0.20105 ± 0.03943`，最终轮为 `0.14682`，最好值为第 189 轮的 `0.26490`。
- 最后 20 轮正确有效共识均值为 `0.17938`，错误有效共识均值为 `0.02167`；共识提升主要来自共同正确，但仍存在共同错误。
- S 与完整测试准确率的逐轮 Pearson 相关系数为 `0.76585`，二者总体同向但并非等价指标。
- 最后 20 轮逐类准确率最低的是标签 5，均值为 `0.76777`；最终轮标签 3 准确率降至 `0.10693`，显示单标签本地更新仍会造成明显的轮间类别偏置。
- 技术报告已通过 Data Analytics 完整清单与有界快照校验，并使用真实来源路径重新校验后成功渲染。
- 本次只有一个训练随机种子，且固定训练客户端仅覆盖 3006 个训练样本，占训练集 `5.01%`；结论仅作描述性陈述，不作统计显著性或因果声明。

## 2026-07-29：SevereTest 三边缘组分层联邦学习

### 实施内容

- 保留原有客户端 0–9 普通 FedAvg 对照流程，新增客户端 0–29 的分层 FedAvg 训练器。
- 固定三个边缘组：客户端 0–9 属于边缘组 0，客户端 10–19 属于边缘组 1，客户端 20–29 属于边缘组 2。
- 每轮 30 个客户端均从上一轮云模型开始训练 1 个本地 epoch；边缘组内按客户端训练样本数聚合，云端再按边缘组训练样本总数聚合三个边缘模型。
- 云端聚合后仍向客户端 0–199 显式下发，并在全部 200 个客户端测试分区上计算总体和逐类指标。
- 探针接口扩展为 30 个客户端、3 个边缘模型和 1 个云模型三层概率，同时保留对原普通 FedAvg 零边缘槽位结果的兼容。
- 分析器改为从元数据动态校验训练客户端数量、边缘组数量、调度和 NPZ 形状，并新增客户端层与边缘层有效共识对比图。
- 新增分层正式配置 `fedml_config_severe_hfl_30clients_200round.yaml` 和两轮冒烟配置 `fedml_config_severe_hfl_30clients_smoke_2round.yaml`；所有 YAML 注释使用英文。
- 更新简体中文说明文档，补充分层拓扑、每轮执行顺序、运行命令和输出说明。

### 测试与冒烟结果

- 单元测试扩展到 10 项并全部通过，新增边缘分组完整覆盖校验和两级样本加权聚合等价性测试。
- 所有新增函数和类方法均具有简体中文说明，代码说明审计未发现缺失项。
- 分层两轮 CPU 冒烟结果目录为 `result/SevereTest/smoke_mnist_single_label_hfl_30clients_3edges_2round_20260729_091538/`。
- 三个边缘组训练样本数分别为 3006、3005、3003，总计 9014，占 MNIST 训练集约 `15.02%`。
- 分层探针张量形状分别为客户端 `[2,30,100,10]`、边缘 `[2,3,100,10]`、云端 `[2,100,10]`；客户端和边缘活跃掩码均全真。
- 两轮调度均固定训练客户端 0–29、激活三个边缘组并向客户端 0–199 全量下发。
- 分析器已从 NPZ 重新计算客户端层和边缘层共识，并与训练期摘要核对一致。
- 原 10 客户端普通 FedAvg 两轮冒烟再次通过，结果目录为 `result/SevereTest/smoke_mnist_single_label_first10_fedavg_2round_20260729_091738/`，证明旧对照流程未被破坏。
- 200 轮分层正式配置和运行入口已完成；本次修改只执行两轮 CPU 冒烟，没有启动耗时较长的 200 轮正式训练。

## 2026-07-29：SevereTest 分层实验改用 GPU

### 修改原因

- 分层正式配置和冒烟配置原先明确设置了 `using_gpu: false`，FedML 因而在服务器存在 GPU 时仍选择 CPU。

### 实施内容

- 将 `fedml_config_severe_hfl_30clients_200round.yaml` 和 `fedml_config_severe_hfl_30clients_smoke_2round.yaml` 修改为 `using_gpu: true`、`gpu_id: 0`。
- 在运行入口增加强制 GPU 校验：当 CUDA 不可用、GPU 编号越界或 FedML 实际设备不是 CUDA 时直接报错，禁止静默回退到 CPU。
- 训练启动日志会输出实际 CUDA 设备和显卡名称，便于服务器端确认。
- 更新简体中文说明文档，增加 CUDA 版 PyTorch、可见 GPU 数量和 `CUDA_VISIBLE_DEVICES` 的检查命令。
- 原 10 客户端普通 FedAvg 对照配置保持 CPU 设置不变。

### 验收

- 两份 HFL YAML 均成功解析为 `using_gpu=True`、`gpu_id=0`。
- 单元测试扩展到 13 项并全部通过，新增 CPU 模式、CUDA 缺失报错和合法 CUDA 设备放行测试。
- 本地环境是 CPU 版 PyTorch，因此未在本机执行 GPU 训练；服务器必须安装 CUDA 版 PyTorch，并确保 `torch.cuda.is_available()` 返回 `True`。
## 2026-07-29：SevereTest 30 客户端分层结果诊断

- 核验了 10 客户端普通 FedAvg 与 30 客户端三边缘组 HFL 两次 200 轮运行的原始逐轮指标和探针结果。
- 确认 HFL 客户端层有效共识 S 的下降主要来自概率一致性 A 下降；同时总体测试准确率、云端探针准确率提高，错误有效共识下降。
- 记录了实验对比的混杂因素：活跃客户端数、训练数据覆盖和 CPU/GPU 同时变化；当前单次边缘聚合的云端结果与 30 客户端扁平样本加权 FedAvg 代数等价。
- 已生成并校验“SevereTest：30 客户端分层与 10 客户端单组的有效共识诊断”数据分析报告，提出受最小样本数约束的 Dirichlet 重采样、多随机种子及同质/异质边缘组对照建议。
