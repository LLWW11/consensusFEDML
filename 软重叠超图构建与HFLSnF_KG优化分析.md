# 非互斥软重叠超图构建及在HFLSnF_KG中的应用分析

> 基于 `Topo_opt/` 与 `HFLSnF_KG_v3/` 全部源代码的深入阅读与分析，不做任何代码修改。

---

## 目录

1. [两个项目的现状：硬分配是怎么来的](#1-两个项目的现状硬分配是怎么来的)
2. [核心问题：从每行一个1到每行多个非零](#2-核心问题从每行一个1到每行多个非零)
3. [软超图构建的五条思路](#3-软超图构建的五条思路)
4. [HFLSnF_KG_v3 的应用发散](#4-hflsnf_kg_v3-的应用发散)
5. [超图神经网络（HGNN）集成的三种想象](#5-超图神经网络hgnn集成的三种想象)
6. [更多发散：跳出框架的思路](#6-更多发散跳出框架的思路)
7. [实施优先级与推荐路径](#7-实施优先级与推荐路径)

---

## 1. 两个项目的现状：硬分配是怎么来的

### 1.1 Topo_opt：最大流 + 单路径截断 = 硬归属

Topo_opt 的核心是一个两阶段最大流问题：先把客户端流量送到边缘节点（C2E），再把边缘节点流量送到云端（E2C）。TSMLG 被展开成 `NL×NL` 的邻接矩阵，虚拟源连客户端（容量1），边缘节点连虚拟汇（容量∞），跑一次 `maxflow` 得到残差图。

**硬分配的根源在 `C2E_Mapping_v4.m`**：拿到残差图后，对每个客户端调一次 `shortestpath`，找到**第一条**到汇的路径，沿路径从汇向源回溯，碰到**第一个**边缘节点就 `break`——这个客户端就永远绑在这个边缘上了。然后沿路径逐边扣 1 单位容量，容量归零的边直接删掉。

这意味着：**即使客户端在拓扑上与多个边缘节点都可达，代码也只选了一个**。残差图中明明还有通往其他边缘的路径，但 `break` 语句把这些信息全部丢弃了。

**Python 侧的双重锁死**：`topology.py` 的 `_normalize_groups()` 检查 `len(set(all_clients)) != len(all_clients)`，`aggregation.py` 的 `merge()` 检查 `contributor_ids` 的交集——两处都会对跨组重复客户端直接 `raise ValueError`。

### 1.2 HFLSnF_KG_v3：纯 TransE + 互斥分区

HFLSnF_KG_v3 在 FB15k-237 上训练 TransE，37 个客户端按头实体贪心均衡分区（互斥）。HFL 架构为"客户端→6个边缘服务器→云端"两级聚合，聚合方式为 `RowCountWeightedFedAvg` + `FedAdam`。拓扑调度直接从 MATLAB 的 `.mat` 文件回放。

**关键事实：项目未使用任何 GNN/HGNN。** 模型是纯嵌入表，无消息传递，客户端之间唯一的间接交互发生在聚合时刻。知识图谱在这里只是训练数据（三元组），不是图结构。

---

## 2. 核心问题：从每行一个1到每行多个非零

### 2.1 形式化

当前状态：关联矩阵 `H ∈ {0,1}^(37×6)`，每行恰有一个 1。

目标状态：`H ∈ [0,1]^(37×6)`，每行非负且行归一化为概率分布。每个客户端可以同时以不同权重"属于"多个边缘组。

### 2.2 为什么需要软重叠

- **信息浪费**：拓扑上一个客户端可能到 3 个边缘节点都只有 2 跳，但硬分配只让它向其中 1 个贡献梯度。其他 2 个边缘组失去了这个客户端的信息。
- **鲁棒性**：边缘节点故障时，硬分配下整组客户端掉线；软分配下该客户端对其他组的贡献仍然有效。
- **非 IID 缓解**：按头实体分区后，客户端之间的数据分布差异很大。跨组重叠可以让相似客户端的梯度在多个边缘组中传播，相当于隐式地做了梯度平滑。
- **为 HGNN 铺路**：软关联矩阵天然就是超图的关联矩阵，可以直接作为超图卷积的输入。硬分配的二值矩阵信息量太低，HGNN 学不到有意义的结构。

### 2.3 两处必须打破的硬约束

| 位置 | 当前逻辑 | 需要改为 |
|------|----------|----------|
| MATLAB `C2E_Mapping_v4.m` | `break` 截断，单路径分配 | 保留多路径信息，输出权重矩阵 |
| Python `aggregation.py` `merge()` | `contributor_ids` 交集检测报错 | 允许跨组重复，改为按权重累加 |

---

## 3. 软超图构建的五条思路

### 思路一：多路径容量感知——"不要 break，继续走"

**直觉**：`C2E_Mapping_v4.m` 的 `break` 是最大的信息瓶颈。把它去掉，让每个客户端枚举到所有可达边缘节点的路径，按路径瓶颈带宽加权和路径长度衰减做加权，就得到了天然的软分配。

**具体做法**：对每个客户端 c 和每个边缘节点 e，在残差图上找 K 条最短路径，每条路径取瓶颈带宽乘以衰减因子 `decay^(跳数-1)`，累加得到 `H_raw[c,e]`，最后行归一化。

**优点**：改动量最小——本质上就是把 `break` 换成 `continue`，再加一个累加器。物理意义清晰：带宽大、路径短的边缘获得更大权重。

**难点**：K-最短路径的计算开销；衰减因子和 K 需要调参。但如果 K 取得不大（比如 3-5），37×6 的规模完全可控。

### 思路二：熵正则化最优传输（Sinkhorn）——"把它当成运输问题"

**直觉**：客户端有"质量"（要发送的梯度），边缘节点有"容量"（能接收的带宽），从客户端到边缘有"运输成本"（拓扑距离）。这是一个经典的最优传输问题。加上熵正则化后，解自然变成软分配矩阵。

**具体做法**：构建成本矩阵 `Cost[i,j]`（综合跳数、带宽倒数、层差），然后用 Sinkhorn 迭代（交替行归一化和列归一化）求解。熵正则化强度 ε 控制软硬程度：ε→0 退化为硬分配，ε→∞ 退化为均匀分配。

**最吸引人的特性**：Sinkhorn 完全可微。这意味着可以把 H_soft 嵌入到 PyTorch 计算图中，让联邦训练的梯度反传到分配矩阵上——分配策略本身可以被学习。这和思路五（可学习拓扑）天然衔接。

**与现有框架的关系**：成本矩阵可以直接从 TSMLG 残差图提取，不需要改 `tsmlg2adj` 或 `add_vnode_vedge` 的任何逻辑。Sinkhorn 只是在 maxflow 之后、mapping 之前插入了一个新的计算步骤。

### 思路三：随机游走扩散——"让信息自己流过去"

**直觉**：在残差图上放一个随机游走者，从客户端出发，按边容量比例随机走 T 步，记录它到达每个边缘节点的概率。走得越多步，信息扩散得越远，软分配就越"模糊"。

**具体做法**：把残差图的加权邻接矩阵转为转移概率矩阵 P（行归一化），然后做 T 步矩阵幂 `P^T`。`H[c,e]` 就是客户端 c 的初始分布经过 T 步扩散后在边缘节点 e 上的概率质量，再乘以时间衰减因子 γ^t。

**有趣的角度**：这其实就是超图上的 PageRank。随机游走视角天然导出超图拉普拉斯 `L_h = I - D_v^{-1/2} H W D_e^{-1} H^T D_v^{-1/2}`，这个拉普拉斯后面 HGNN 直接要用。

**与思路二的区别**：Sinkhorn 是"全局优化"（在所有分配上找最优），随机游走是"局部扩散"（信息只沿图结构传播）。随机游走更适合"拓扑结构本身就是软分配的主要信息源"的场景。

### 思路四：谱聚类模糊隶属度——"让数据说话"

**直觉**：如果两个客户端在拓扑上很近、在数据分布上也很近，那它们"应该"被分到同一组。谱聚类先构建相似度矩阵，再做特征分解和模糊 C-均值，得到的就是软隶属度。

**独特价值**：这是唯一一个可以**融合拓扑信息和语义信息**的方法。相似度矩阵可以同时包含：
- 拓扑维度：最短路径跳数、最大流值、共享边缘节点数
- 语义维度：客户端实体集合的 Jaccard 相似度、训练中动态更新的嵌入余弦相似度、梯度方向相似度

**问题**：N=37 的规模做谱分解没有计算问题，但谱聚类本身是离线的、静态的。如果要动态调整，需要和思路二或思路五结合。

### 思路五：超图拉普拉斯正则化反推——"让相似的客户端有相似的模型"

**直觉**：反过来想——我们不直接定义 H，而是定义一个目标："在超图上相近的客户端应该有相似的模型参数"。这个目标可以写成超图拉普拉斯正则项 `tr(F^T L_h F)`，然后优化 H 使这个正则项最小。

**哲学层面的转变**：前四种方法都是"先定 H，再做联邦学习"；这个方法是"让联邦学习的目标函数自己决定 H 应该长什么样"。H 不再是预设的拓扑映射，而是学习目标的一部分。

**实践中的挑战**：`L_h` 是 H 的非线性函数，需要交替优化（固定 H 算 L_h，固定 L_h 更新 H）。但 37×6 的规模下，这个优化是可行的。

### 五种方法的对比

| 维度 | 多路径容量 | Sinkhorn | 随机游走 | 谱聚类 | 拉普拉斯优化 |
|------|:---:|:---:|:---:|:---:|:---:|
| 与现有代码的兼容性 | 最高 | 高 | 高 | 中 | 低 |
| 可微/可端到端学习 | 否 | **是** | 否 | 否 | **是** |
| 能融合语义信息 | 否 | 成本矩阵中可加 | 否 | **天然支持** | 正则项中可加 |
| 超参数数量 | 2 | 1 | 2 | 3 | 2 |
| 计算复杂度 | 低 | 低 | 中 | 中 | 中 |
| **推荐度** | ★★★★★ | ★★★★★ | ★★★ | ★★ | ★★★ |

**推荐组合**：先用思路一（多路径容量）做快速验证，确认软分配确实有效后，再切到思路二（Sinkhorn）获得可微性和端到端学习能力。

---

## 4. HFLSnF_KG_v3 的应用发散

### 4.1 第一层：软聚合——"客户端向多个边缘组按比例贡献"

最直接的应用。当前聚合逻辑是：客户端 i 的完整更新只贡献给它归属的边缘组 j。软聚合改为：客户端 i 的更新按 `H[i,j]` 的权重**同时**贡献到多个边缘组。

**具体影响**：
- `RowCountWeightedFedAvgAggregator.accumulate()` 中，每个客户端的 `row_counts` 乘以 `H[i,j]` 后累加到边缘组 j 的统计量中
- `merge()` 中不再检测 `contributor_ids` 互斥，而是允许同一客户端出现在多个边缘统计中（权重不同）
- `finalize()` 的归一化逻辑不变——分母自然变成了各边缘组的加权出现次数之和

**为什么这会有用**：在非 IID 场景下，按头实体分区意味着不同客户端的实体嵌入更新方向差异很大。如果客户端 A（拥有实体 X）和客户端 B（拥有实体 Y）在拓扑上很近但被分到不同边缘组，硬分配下它们的梯度永远不会在同一次边缘聚合中相遇。软分配让 A 的一部分梯度也流向 B 所在的边缘组，相当于在边缘层做了隐式的跨组梯度混合。

### 4.2 第二层：可学习软分配——"让训练过程自己决定谁跟谁一组"

固定拓扑成本矩阵算出来的 H_soft 是静态的。但如果我们在 PyTorch 里维护一组客户端嵌入和边缘嵌入，用注意力机制（`client_emb @ edge_emb.T / temperature`）生成 H_soft，就可以让联邦训练的梯度反传到这些嵌入上。

**核心洞察**：当前的 MATLAB 拓扑调度是"先优化网络拓扑，再做联邦学习"的两阶段方法。可学习软分配把这两个阶段**融合成一个端到端优化问题**——拓扑选择本身成为联邦学习目标函数的可微部分。

**训练信号从哪来**：联邦学习每轮的验证 MRR 就是 reward。可以用元学习（MAML）的思路：内循环做客户端本地训练，外循环优化软分配参数使得验证 MRR 最大化。或者更简单——用 REINFORCE 把软分配看作随机策略，用 MRR 提升作为奖励信号。

**有趣的风险**：如果完全不加约束，可学习软分配可能会退化为"把所有客户端都分给一个边缘组"（因为这样聚合最简单）。所以需要加拓扑先验作为正则（`H_final = α · H_learned + (1-α) · H_topology_prior`），或者用 Sinkhorn 层保证行列约束。

### 4.3 第三层：超图正则化——"在损失函数里加一项"

不改变聚合方式，只在客户端本地训练的损失函数中加一个超图拉普拉斯正则项 `λ · tr(F^T L_h F)`，其中 F 是客户端的模型参数矩阵，L_h 是从软关联矩阵 H 构建的超图拉普拉斯。

**物理意义**：这个正则项惩罚"在超图上相近的客户端拥有差异很大的模型参数"。换句话说，它鼓励拓扑上（或语义上）相近的客户端保持模型一致性，即使它们被分到不同的边缘组。

**与现有训练循环的关系**：TransE 的本地训练已经有自对抗逻辑损失，超图正则项只是加了一项 L2-like 的惩罚。不需要改聚合器，不需要改拓扑调度，只需要在 `model_trainer.py` 的 loss 计算中多加一项。但代价是每轮需要把当前的全局模型参数分发下去作为 L_h 的参考点——通信开销略增。

### 4.4 第四层：动态超图演化——"超图本身随训练进化"

到目前为止讨论的 H_soft 都是每轮重新计算的（但计算方式固定）。更激进的想法是：让超图结构本身**随训练进度演化**。

**训练前期的超图应该"宽"**：熵正则化强（ε 大），客户端广泛参与多个边缘组，相当于探索阶段——让信息充分混合，避免过早陷入局部最优。

**训练后期的超图应该"窄"**：熵正则化弱（ε 小），软分配逐渐收敛为近似硬分配，相当于利用阶段——让每个客户端专注于贡献给它最擅长的边缘组。

**类比**：这和学习率衰减是同一个道理——前期探索，后期利用。只不过这里衰减的不是学习率，而是超图的"模糊度"。

### 4.5 第五层：多视图超图——"拓扑视图 + 语义视图 + 结构视图"

**拓扑视图**：从 TSMLG 残差图计算的成本矩阵（思路一~三），反映网络物理可达性。

**语义视图**：从 FB15k-237 数据分区计算的客户端相似度。两个客户端如果拥有大量共同实体或关系，即使拓扑距离远，也应该有较高的软关联权重。具体可以用实体集合的 Jaccard 系数、关系分布的 KL 散度、或训练中动态更新的嵌入余弦相似度。

**结构视图**：从知识图谱本身的图结构提取。FB15k-237 是一个有向图，实体之间通过关系连接。如果客户端 A 拥有的实体在 KG 中与客户端 B 拥有的实体直接相连（通过某条关系），那 A 和 B 在联邦学习中应该有更强的协作关系——因为它们的实体嵌入需要协调才能正确建模那条关系。

**融合方式**：`H_final = α · H_topology + β · H_semantic + γ · H_structural`，其中 α+β+γ=1。或者更灵活地，用注意力机制让模型自己学习三个视图的权重。

---

## 5. 超图神经网络（HGNN）集成的三种想象

### 5.1 为什么要引入 HGNN

当前 HFLSnF_KG_v3 的三个瓶颈：

1. **客户端之间无信息交互**——只在聚合时通过边缘组间接交互，且交互方式是简单的加权平均
2. **边缘服务器只是"管道"**——做 FedAvg 聚合，不具备推理能力，不能智能地过滤或变换客户端梯度
3. **TransE 不利用图结构**——纯嵌入表，没有消息传递，知识图谱的图结构信息完全被浪费

HGNN 可以同时解决这三个问题。

### 5.2 想象 A：HGNN 替代边缘聚合器（改动最小）

**核心思路**：把边缘服务器的 FedAvg 聚合替换为超图卷积。客户端的模型参数/梯度作为节点特征 X，软关联矩阵 H 作为超图结构，超边权重 W 来自边缘节点的带宽/容量。经过 2-3 层 HGNN 卷积后，输出一个"增强的全局表示"作为云端更新。

**超图卷积的核心公式**：`X' = σ(D_v^{-1/2} H W D_e^{-1} H^T D_v^{-1/2} X Θ)`

这个公式的直觉是：每个客户端的特征先聚合到它所属的超边（边缘组）上（`H^T X`），超边上的聚合结果再传播回客户端（`H · ...`），形成两步消息传递。与普通 FedAvg 的区别在于：FedAvg 只做一次加权平均，HGNN 做了多跳传播且每跳都有可学习变换。

**为什么比 FedAvg 好**：FedAvg 是"所有客户端等权平均"或"按数据量加权平均"，不考虑客户端之间的关系。HGNN 的消息传递让"在超图上相近的客户端"的梯度获得更大的相互影响权重，相当于做了**结构感知的梯度混合**。

**改动范围**：只动聚合层（新增 `hgnn_aggregator.py`），TransE 模型和本地训练循环不变。云端需要多存一个 HGNN 的小型神经网络参数。

### 5.3 想象 B：HypergraphTransE——超图增强的 KGE 模型（改动最大）

**核心思路**：在 TransE 的实体嵌入表上叠加超图卷积层。实体嵌入不再只是查表，而是"查表 + 超图增强"的融合表示。

**信息流**：
1. 每个客户端拥有一批实体的原始嵌入（TransE 基础嵌入）
2. 按客户端数据分区构建"客户端-实体"关联矩阵
3. 客户端级别的表示通过 HGNN 在软超图上传播，生成"增强的客户端嵌入"
4. 增强的客户端嵌入回传到实体级别（通过客户端-实体关联矩阵的转置），生成"增强的实体嵌入"
5. TransE 评分函数使用"原始嵌入 + 增强嵌入"的融合

**为什么有价值**：知识图谱嵌入的核心挑战是"如何让没有直接交互的实体通过多跳关系获得间接信息"。普通 TransE 完全没有这个能力——每个实体的嵌入只被它出现的三元组训练。HGNN 增强后，如果实体 X 属于客户端 A，客户端 A 在超图上与客户端 B 相近（因为数据分布相似或拓扑相近），那么实体 X 的嵌入就会受到客户端 B 拥有的实体的影响——即使 X 和那些实体从未在同一个三元组中出现过。

**风险与代价**：改动模型结构意味着需要重新设计训练循环、负采样逻辑、评估流程。HGNN 层的引入会增加每轮的计算和通信开销（需要传递超图结构和中间特征）。但如果 MRR 提升显著，这个代价是值得的。

### 5.4 想象 C：HGNN 作为自适应拓扑控制器（最有想象力）

**核心思路**：不改变 TransE 模型，也不改变聚合方式，而是用 HGNN 来**学习最优的软分配矩阵 H_soft**。HGNN 在这里不是做特征变换，而是做**拓扑决策**。

**工作流程**：
1. 每轮训练开始前，收集所有活跃客户端的"状态信号"：本地损失、梯度范数、数据量、实体数、与他人的实体重叠度等
2. 这些信号编码为客户端特征向量，输入 HGNN
3. HGNN 在**上一轮的软超图**上传播这些特征，输出每个客户端对每个边缘组的 logits
4. Softmax + 温度调节 → 新的 H_soft
5. 用新的 H_soft 做本轮的软聚合
6. 训练完成后，用验证 MRR 的变化作为反馈信号，更新 HGNN 参数

**为什么这个方案最有想象力**：它把"拓扑优化"和"联邦学习"彻底融为一体。当前的 Topo_opt 是用最大流离线优化拓扑，HFLSnF_KG_v3 是在线回放拓扑。想象 C 让拓扑本身成为联邦学习过程中的**可学习组件**——拓扑不再是一个预设的约束，而是一个被训练的策略。

**与 Topo_opt 的关系**：Topo_opt 的最大流结果可以作为 HGNN 控制器的**先验**（`H_prior`），HGNN 在这个先验基础上做微调。这样既保留了物理拓扑的约束（不可能的路径不会被选），又加入了数据驱动的人工智能优化。

---

## 6. 更多发散：跳出框架的思路

### 6.1 超边权重学习——"不是所有边缘组都同等重要"

当前所有边缘组的地位是平等的，都是简单做 FedAvg。但如果某个边缘组汇聚的客户端数据质量更高（比如三元组更"干净"），或者某个边缘节点的网络条件更好（带宽大、延迟低），那它应该获得更大的聚合权重。

**做法**：给每条超边一个可学习的权重 `w_j`，在超图拉普拉斯中作为 W 对角阵。这个权重可以是一个简单的标量（每轮学习），也可以是边缘节点特征的函数（`w_j = MLP(edge_features_j)`）。

### 6.2 动态超边数量——"不是总需要6个组"

当前固定 6 个边缘节点，但不同轮次可能需要不同数量的组。训练初期数据分布混乱，可能需要更多组来并行探索；训练后期模型趋于收敛，可能需要更少组来加强聚合。

**做法**：把超边数量也作为可学习参数。用稀疏正则化（L1 on column sums of H）让某些超边的权重逐渐趋零，等效于"关闭"该边缘组。或者用门控机制（gumbel-softmax）做离散的组数选择。

### 6.3 超图注意力——"不是所有客户端对超边的贡献都一样"

软分配矩阵 H 只定义了"客户端属于边缘组的程度"，但没有区分"客户端在这个组里贡献什么"。可以引入超图注意力机制：对每对 (客户端 i, 超边 j)，不仅有权重 H[i,j]，还有注意力系数 α[i,j] 决定客户端 i 的**哪些参数行**对超边 j 更重要。

**与 RowCountWeighted 的关系**：当前的 `row_counts` 已经是逐行的权重（按实体出现次数）。超图注意力可以看作是 row_counts 的**学习版本**——不再只看出现次数，还看梯度方向、参数重要性等信号。

### 6.4 跨轮超图记忆——"超图应该有惯性"

当前每轮的拓扑是独立的（从 MAT 文件逐轮回放）。但联邦学习有强烈的时序依赖——上一轮的模型状态会影响这一轮的梯度方向。超图也应该有"惯性"：上一轮分配得好的 H_soft 应该对这一轮有影响。

**做法**：引入超图动量 `H_t = β · H_{t-1} + (1-β) · H_new_t`，或者用 RNN/LSTM 在超图序列上做时序建模。这让超图不再是无状态的，而是随着训练进程"记住"好的分配模式。

### 6.5 对比学习视角——"用超图定义正样本对"

联邦学习中一个核心难题是非 IID 数据导致的客户端 drift。超图结构可以用来定义"哪些客户端应该相似"——在超图上同一超边的客户端是正样本对，不同超边的客户端是负样本对。在本地训练中加入对比学习损失，鼓励正样本对的模型参数接近，推开负样本对。

**与超图正则化的区别**：超图正则化是直接在参数空间施加约束（`tr(F^T L_h F)`），对比学习是在表示空间施加约束（InfoNCE loss）。两者可以叠加使用。

### 6.6 超图 + 知识图谱的对偶——"两层图结构"

HFLSnF_KG_v3 实际上有两层图结构：
- **物理层**：客户端-边缘-云的网络拓扑（超图）
- **语义层**：FB15k-237 知识图谱的实体-关系图

这两层图是**对偶**的：物理层的超边定义了客户端之间的协作关系，语义层的知识图谱边定义了实体之间的语义关系。客户端-实体分区矩阵把这两层图连接起来。

**大胆的想法**：在这两层图上做**联合消息传递**。物理超图上的 HGNN 传播客户端表示，语义知识图谱上的 GNN 传播实体表示，两者通过客户端-实体关联矩阵交互。这相当于让"网络拓扑结构"和"知识图谱结构"互相增强——物理上近的客户端共享语义信息，语义上相关的实体影响物理拓扑的优化。

---

## 7. 实施优先级与推荐路径

### 7.1 推荐路径

```
阶段 0：基础建设
│  Topo_opt 侧：新增 C2E_Mapping_Soft.m（思路一：多路径容量感知）
│  输出 H_soft.mat 替代原来的 c2emap
│  
│  HFLSnF_KG_v3 侧：新增软关联矩阵解析逻辑
│  放宽 topology.py 和 aggregation.py 的互斥校验
│
▼
阶段 1：软聚合验证
│  用 H_soft 做加权聚合，TransE 模型不变
│  消融实验：硬分配 vs 软分配，不同 ε/decay
│  ★ 关键验证点：MRR 是否有提升？重叠度与性能的关系？
│
▼
阶段 2：可学习拓扑
│  引入注意力机制生成 H_soft（思路二：Sinkhorn 可微层）
│  训练中动态调整分配策略
│  ★ 关键验证点：可学习分配是否优于静态分配？
│
▼
阶段 3：HGNN 集成
   想象 A（首试）：HGNN 替代 FedAvg 聚合器，模型不变
   想象 C（进阶）：HGNN 自适应拓扑控制器
   想象 B（高级）：HypergraphTransE 模型
```

### 7.2 关键改动文件清单

**MATLAB 侧（Topo_opt）**

| 文件 | 改动 |
|------|------|
| 新增 `C2E_Mapping_Soft.m` | 基于 `C2E_Mapping_v4.m`，去掉 `break`，多路径累加 |
| 新增 `HFL_SnF_Soft_v1.m` | 基于 `HFL_SnF_v5.m`，输出 H_soft 到 .mat |
| `tsmlg2adj.m`, `add_vnode_vedge_v2.m` | 无需改动 |

**Python 侧（HFLSnF_KG_v3）**

| 文件 | 改动 |
|------|------|
| `core/topology.py` | 放宽 `_normalize_groups` 互斥校验；新增软拓扑 dataclass |
| `core/matlab_topology_schedule.py` | 新增软关联矩阵解析 |
| `core/aggregation.py` | 放宽 `merge()` 的 `contributor_ids` 检查；新增软加权聚合 |
| `fedml_kge/dynamic_trainer.py` | 训练循环接入软聚合 |
| 新增 `core/hgnn_aggregator.py` | HGNN 卷积层 + 聚合器 |
| 新增 `core/learnable_topology.py` | 可学习软分配模块 |
| 新增 `core/hypergraph_regularizer.py` | 超图拉普拉斯正则化 |
| `configs/*.yaml` | 新增软拓扑相关配置项 |

### 7.3 消融实验矩阵

| 实验臂 | 拓扑 | 聚合方式 | 模型 | 预期效果 |
|--------|------|----------|------|----------|
| Baseline | 硬分配 | RowCountWeighted | TransE | 已有结果 |
| Soft-Static | 软分配(静态) | SoftRowCountWeighted | TransE | MRR +0.5~2% |
| Soft-Learnable | 软分配(可学习) | SoftRowCountWeighted | TransE | MRR +1~3% |
| HGNN-Agg | 软分配(静态) | HGNN Aggregator | TransE | MRR +2~5% |
| HGNN-Full | 软分配(可学习) | HGNN Aggregator | HypergraphTransE | MRR +3~8% |

### 7.4 核心观测指标

- 验证 MRR / Hits@1/3/10（主要）
- 软分配矩阵的熵（衡量"模糊度"）
- 每轮跨组重叠客户端比例
- 训练收敛速度（达到目标 MRR 所需轮数）
- 客户端参与均衡度（是否某些客户端被边缘化）

---

## 附录：核心数学符号表

| 符号 | 含义 | 维度 |
|------|------|------|
| N | 客户端数量 | 37 |
| M | 边缘节点/超边数量 | 6 |
| L | TSMLG 层数 | 8 |
| H | 软关联矩阵 | [N, M] |
| W | 超边权重对角阵 | [M, M] |
| D_v | 顶点度对角阵 | [N, N] |
| D_e | 超边度对角阵 | [M, M] |
| L_h | 归一化超图拉普拉斯 | [N, N] |
| X | 节点特征矩阵 | [N, d] |
| ε | Sinkhorn 熵正则化强度 | 标量 |

---

*文档生成时间：2026-07-29*
*基于 Topo_opt (50+ .m 文件) 和 HFLSnF_KG_v3 (40+ .py 文件) 全部源代码分析*
