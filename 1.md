这几篇文章都提到了共识，但是共识实际上分为两大类：

1. **控制理论意义上的严格数值共识**：最终状态确实要求相同或渐近相同；
2. **LLM 推理意义上的答案共识**：重点是答案类别、核心结论或决策结果一致，而不是完全相同

| 文章                  | 共识对象             | 是否要求所有智能体完全一致 | 是否等于多数投票         | 是否要求文本完全相同   |
| --------------------- | -------------------- | -------------------------- | ------------------------ | ---------------------- |
| Co-Learning           | 神经网络参数         | 是，参数渐近趋同           | 否                       | 不涉及文本             |
| 层次多智能体系统共识  | 智能体物理状态       | 是，跟随者趋于领导者状态   | 否                       | 不涉及文本             |
| LLM Consensus Seeking | 数值位置或数值状态   | 是，最终状态相同           | 否                       | 文本只是生成数值的工具 |
| DyLAN                 | 标准化答案或相似回答 | 不一定，超过阈值即可       | 接近超多数一致           | 不要求逐字相同         |
| DySCo                 | 答案类别及其置信分布 | 不一定，低熵且稳定即可     | 是加权多数，不是简单多数 | 不要求逐字相同         |

这就导致了有些割裂：

- HFL 操作的是**模型参数、梯度或表示向量**；
- IoA 中面向任务的共识往往操作的是**意见、答案、状态、决策或动作**

### HFL

HFL 是分层联邦优化过程，其中包含周期性的模型聚合与参数同步，可以解释为模型空间或知识空间中的层级一致化，但它不等同于经典分布式控制中的渐近严格共识

其中每个智能体通过邻居状态更新自身状态，最终所有状态渐近一致。

而 HFL 中通常是：

 **本地训练——边缘聚合——云端聚合——参数下发**


如果所有参与客户端都完全覆盖那么在参数下发的瞬间所有模型参数相等

这是一种**周期性精确同步**，但一旦重新进行本地训练，非 IID 数据会使客户端模型再次发生漂移

因此，标准 HFL 更适合描述为：

- 周期性模型同步；
- 分层知识聚合；
- 模型状态协调；
- 联邦优化收敛；
- 模型空间中的近似或周期性一致化。

不能直接写成：HFL is a strict consensus process.

更稳妥的是：HFL实现了层次模型状态同步和知识聚合，这可以理解为模型空间中的一种共识形式

CoLearning 论文提供了一个更严格的桥梁：它确实将神经网络的每一个权重视为共识变量，让智能体通过邻居交换和迭代更新收敛到平均模型。也就是说，**CoLearning 是“把经典共识算法应用到学习参数上”**，而普通 FedAvg/HFL 是“服务器计算平均值后统一下发”。二者结果可能类似，但机制不同

### IOA

IoA 中的共识比传统 HFL 更接近任务层面的答案、意见和决策共识

对于 LLM 智能体，重点会进一步扩展到推理路径、置信度、候选答案和语义结论的协调。该综述也明确提出大规模 IoA 可以使用层级共识：先在局部集群中形成局部共识，再由集群代表向上聚合形成全局共识。同时，IoA 中交换的信息并不只是自然语言。IoA 综述指出，智能体之间交换的对象可以包括模型参数、加密令牌和潜在表示，因此模型训练和参数聚合本身也可以成为 IoA 的基础能力。([arXiv](https://arxiv.org/html/2505.07176v2))

所以，最好把 IoA 中的共识分为三个层次：

| 共识层次   | 共识对象                         | 典型机制                   |
| ---------- | -------------------------------- | -------------------------- |
| 模型级共识 | 参数、梯度、共享表示、适配器     | FL、HFL、去中心化学习      |
| 知识级共识 | 原型、规则、经验、记忆、推理洞见 | 知识蒸馏、语义聚合、经验库 |
| 决策级共识 | 标签、答案、计划、动作           | 投票、加权聚合、辩论、协商 |

HFL 实验属于第一层；LLM 多智能体推理通常属于第三层。二者不是完全割裂，而是不同层次的共识。因此，不能说“用联邦学习替代智能体共识”，**应当说“用 HFL 实现 IoA 的学习平面共识”**

**不推荐的叙述**

> IoA 中的智能体需要形成任务答案共识，因此我们使用 HFL 的模型参数聚合来替代答案共识

这个逻辑不成立，因为即使所有智能体拥有相同模型，如果输入、提示、采样随机性、外部知识或工具不同，仍然可能产生不同答案，反过来多个结构完全不同的智能体也可能给出相同答案。

**推荐的叙述**

> 本文不将联邦学习等同于 IoA 中的全部共识过程，而是将 HFL 视为 IoA 学习平面中的层级知识共识机制。终端智能体基于私有数据形成局部知识，边缘代理完成域内知识聚合，云端协调代理完成跨域知识聚合与能力同步。共享模型进一步为执行阶段的任务级决策共识提供共同的知识基础。

可以将系统明确划分为两个平面：

**学习平面：慢时间尺度**

$\text{本地学习}\rightarrow\text{边缘知识共识}\rightarrow\text{云端全局知识协调}\rightarrow\text{能力同步}$

此时采用 HFL

**执行平面：快时间尺度**

$\text{局部感知}
\rightarrow
\text{独立预测}
\rightarrow
\text{边缘意见聚合}
\rightarrow
\text{全局决策共识}$

此时采用标签、概率向量或置信度聚合，这就不是“拿训练代替推理”，而是：

> **训练阶段形成共享知识基础，执行阶段在共享知识基础上形成任务决策。**

近期的 Federation of Agents 论文也专门区分了联邦学习和运行时智能体协同：前者负责隐私保护的模型训练，后者负责异构智能体的发现、调度和协作；但该框架允许把 FL 作为可选的训练阶段嵌入智能体协同流程。这个观点非常适合支撑你的论文定位。([arXiv](https://arxiv.org/html/2509.20175v1))

## 如何把现有 MNIST/FEMNIST 实验迁移到 IoA ？

不一定要立刻改成 LLM 推理实验，可以把当前分类模型封装为**感知与分类智能体**，IoA 并不只包含语言智能体，也包括终端设备、机器人、感知智能体和边缘代理。

### 1. 重新定义系统角色

| 原 HFL 节点  | IoA 中的角色              | 功能                             |
| ------------ | ------------------------- | -------------------------------- |
| Client       | 终端感知/学习智能体       | 本地训练、图像识别、生成局部意见 |
| Edge Server  | 子 IoA 网关代理或中继代理 | 域内聚合、可靠性评估、意见融合   |
| Cloud Server | 全局协调智能体            | 跨域知识聚合、全局决策、策略下发 |
| Local Model  | 智能体本地知识状态        | 表示智能体从私有环境中学习的能力 |
| Model Update | 知识消息                  | 智能体向上层共享的压缩知识       |
| Global Model | 共享能力模型              | IoA 中跨智能体形成的知识基础     |

IoA 原始框架强调连接异构第三方智能体、分布式运行、动态组队和通信流控制；后续 IoA 综述进一步引入了子 IoA、网关代理和跨域状态同步。这与终端—边缘—云的组织方式具有较强的结构对应关系。([arXiv](https://arxiv.org/abs/2407.07061))

但不能只是把 `client` 改名为 `agent`。每个终端智能体至少应具有：

- 独立局部数据或观测；
- 独立模型状态；
- 自主参与或退出能力；
- 根据本地模型执行预测；
- 与其他智能体或网关交换消息；
- 根据收到的全局信息更新自身状态。

### 2. 保留现有 HFL 训练，再增加一个很轻量的“决策共识实验”

对同一测试样本$(x)$ ，让不同客户端模型分别输出概率向量：$ p_i(x) = [p_i(0|x),p_i(1|x),\ldots,p_i(9|x)]$ 

边缘代理进行局部意见融合：$ p_e(x) = \sum_{i\in\mathcal C_e}\alpha_i p_i(x)$ 

云端协调代理进行全局融合：$ p_g(x) = \sum_{e=1}^{E}\beta_e p_e(x)$ 

最终全局共识答案为：$\hat y(x)=\arg\max_y p_g(y|x)$ 

其中 $ \alpha_i$  和 $ \beta_e$ 可以是：数据量权重、本地验证准确率权重、置信度权重、历史可靠性权重、当前样本上的校准置信度权重等

与此同时，建议增加共识指标，只报告 accuracy 和 loss，审稿人仍然会认为这是普通 HFL 论文，要加入直接反映“共识”的指标，比如:

1) **参数分歧度**
$D_{\theta}^{r} = \frac{1}{M}\sum_{i=1}^{M}|\theta_i^r-\theta_g^r|_2^2$

衡量本地模型与全局模型之间的参数差异

也可以分成：$D_{\mathrm{edge}}=\sum_e\sum_{i\in\mathcal C_e}|\theta_i-\theta_e|^2$和$D_{\mathrm{global}}=\sum_e|\theta_e-\theta_g|^2$

分别刻画域内共识和跨域共识

2) **输出分歧度**

对每个测试样本计算不同智能体概率分布之间的 Jensen–Shannon 散度：$ D_p(x) = \frac{1}{M}\sum_iJS(p_i(x)|p_g(x))$

如果 HFL 训练后 $D_p$ 明显下降，可以说明共享知识促进了任务输出一致性

3) **共识比例**

$A(x) = \frac{\max_y\sum_i w_i\mathbf 1[\hat y_i(x)=y]}{\sum_i w_i}$，例如 $A(x)\geq 2/3$ 时认为形成软共识。

4) **共识熵**

$ H(x) = -\sum_y q(y|x)\log q(y|x)$，其中 $q(y|x)$ 是各智能体投票形成的标签分布

- 熵高：意见分散；
- 熵低：意见集中；
- 熵低且错误：错误共识

5) **错误共识率**

$R_{\mathrm{wrong}}P(\hat y_g\neq y^\ast,\ A(x)\geq\tau)$



这样就同时拥有：**训练阶段的模型级共识**和**推理阶段的答案级共识**

对于 MNIST/FEMNIST，答案共识就是对数字或字符标签的共识，而不是自然语言文本共识，这在概念上完全成立

###一个适合你当前工作的实验框架

建议至少设置以下对比：

| 方法                           | 训练聚合   | 推理聚合                   |
| ------------------------------ | ---------- | -------------------------- |
| Local Only                     | 无         | 无                         |
| Flat FedAvg                    | 云端       | 无                         |
| HFL                            | 边缘＋云端 | 无                         |
| Local Decision Voting          | 无         | 标签多数投票               |
| HFL + Flat Decision            | 边缘＋云端 | 云端直接融合               |
| HFL + Hierarchical Consensus   | 边缘＋云端 | 边缘意见共识＋云端全局共识 |
| HFL + Trust-Weighted Consensus | 边缘＋云端 | 可靠性加权层级共识         |

再设置四类环境：

1. IID；
2. MNIST 标签非 IID；
3. FEMNIST 书写者非 IID；
4. 动态 IoA：随机智能体退出、加入或切换边缘节点。

IoA 具有动态创建、迁移、终止以及子网络重构等特点，因此加入动态参与实验会明显增强 IoA 叙述的可信度。([arXiv](https://arxiv.org/html/2505.07176v2))


## 审稿人能够接受的论文表述

可以在论文中这样写：

> 本文并不将联邦优化等同于 IoA 中的语义推理共识。相反，我们将 IoA 共识划分为学习平面的知识共识和执行平面的决策共识。在学习平面中，终端智能体基于私有数据形成局部模型状态，边缘代理和云端协调代理通过分层联邦聚合形成共享知识表示。在执行平面中，各智能体基于其模型和局部观测生成任务意见，随后通过分层概率聚合形成全局决策。由此，HFL 不是对任务级智能体协商的简单替代，而是支持大规模 IoA 协同决策的隐私保护知识协调机制。

##  相关文献及其在你论文中的作用

| 文献                                                         | 可以支持的论点                                               |
| ------------------------------------------------------------ | ------------------------------------------------------------ |
| Carrascosa et al., **Consensus-Based Learning for MAS: Definition, Implementation and Integration in IVEs**, 2023 | 明确把神经网络权重作为多智能体共识变量，是 FL 与经典共识理论之间最直接的桥梁 |
| Carrascosa et al., **Asynchronous Consensus for Multi-Agent Systems and Its Application to Federated Learning**, EAAI 2024 | 用异步多智能体共识实现无中心 FL，支持“学习过程可以视为模型状态共识”的观点。([ScienceDirect](https://www.sciencedirect.com/science/article/pii/S0952197624009989)) |
| Chen et al., **Internet of Agents: Weaving a Web of Heterogeneous Agents for Collaborative Intelligence**, 2024 | IoA 的异构智能体接入、分布式环境、动态组队和通信流控制。([arXiv](https://arxiv.org/abs/2407.07061)) |
| Wang et al., **Internet of Agents: Fundamentals, Applications, and Challenges**, IEEE TCCN 2025 | IoA 分层架构、子 IoA、网关代理、模型参数传输、决策共识和层级共识，是最重要的 IoA 背景文献。([arXiv](https://arxiv.org/html/2505.07176v2)) |
| Chen et al., **Multi-Agent Consensus Seeking via Large Language Models**, 2025 | 将数值状态看作更复杂解空间中的抽象点，连接经典平均共识和 LLM 智能体协商。 |
| Giusti et al., **Federation of Agents: A Semantics-Aware Communication Fabric for Large-Scale Agentic AI**, 2025 | 明确区分 FL 训练和智能体运行时编排，同时说明 FL 可以作为智能体联邦的训练阶段。([arXiv](https://arxiv.org/html/2509.20175v1)) |
| Yao et al., **Federation over Text: Insight Sharing for Multi-Agent Reasoning**, 2026 | 提出“参数联邦”与“语义联邦”的区别：不是聚合梯度，而是聚合推理过程和元认知洞见，是连接 HFL 与语义共识最有价值的近期文献。([arXiv](https://arxiv.org/abs/2604.16778)) |

------

## 最终建议

你现有的实验不应被包装成“用 HFL 替代 IoA 推理共识”，而应升级为：

> **HFL 实现模型/知识层的层级共识，额外的层级输出聚合实现任务/决策层共识**

至少需要增加三项内容：

1. 把终端、边缘和云分别建模为学习智能体、网关代理和协调代理；
2. 在现有 MNIST/FEMNIST 实验上增加概率输出或标签的边缘—云层级共识；
3. 同时报告参数分歧、输出分歧、共识熵和错误共识率。

完成这三项后，你的工作不再只是把 HFL 换成 IoA 名称，而是形成一条完整逻辑链：

$
\text{私有局部经验}\rightarrow\text{分层知识聚合}\rightarrow\text{共享能力形成}\rightarrow\text{异构局部意见}\rightarrow\text{分层任务共识}
$

这条路径在概念和实验上都更容易得到审稿人认可
