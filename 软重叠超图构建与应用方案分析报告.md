# 非互斥软重叠超图构建与应用方案分析报告

> 日期：2026-07-29
> 基于：`D:/1/1myworkcode/Topo_opt/`（MATLAB）和 `D:/1/1myworkcode/HFLSnF_KG_v3/`（Python）

---

## 一、两个项目的核心逻辑回顾

### 1.1 Topo_opt（MATLAB）的硬分配机制

整个项目的核心链路：

```
TSMLG 时隙多层图 → tsmlg2adj 展平为 NL×NL 邻接矩阵
→ add_vnode_vedge 添加虚拟源/汇 → maxflow 求最大流
→ C2E_Mapping 提取客户端→边缘映射
```

**硬分配的关键瓶颈在 `C2E_Mapping` 系列函数。**

以 `C2E_Mapping.m`（v1）为例：

```matlab
for 每个客户端 s:
    P = shortestpath(G, s, 虚拟汇)
    for 路径 P 上的每个节点:
        if 节点 属于 EdgeSet:
            令 s 归属此边缘组 → break  %% ← 这就是硬分配的根源！
```

- v1：从客户端出发正向找第一个边缘节点 → break
- v2：从虚拟汇反向搜索 → 仍然是找到第一个就 break
- v3：增加带宽扣除验证 → 每客户端仍只分配一次
- v4：增加层维度记录 → 每客户端仍只分配一次

**关联矩阵本质上是一个 0-1 矩阵，每行有且仅有一个 1。**

因此，如果你要让每行不止一个非零元素，核心改造就在 `C2E_Mapping` —— 不 `break`，而是收集路径上（或可达范围内）的多个边缘节点，并计算归属权重。

### 1.2 HFLSnF_KG_v3（Python）的硬分配机制

硬分配同样被严格执行。`_normalize_groups`（`core/topology.py` 第 12-35 行）：

```python
if len(set(all_clients)) != len(all_clients):
    raise ValueError("同一客户端不能同时出现在多个边缘组")
```

聚合流程也基于硬分组：

```
client → accumulate(组内求和) → merge(组间合并) → finalize(归一化)
```

`group_to_client_indexes` 是一个 `Dict[int, Tuple[int, ...]]`，每个 client_id 只在唯一一个 group 中出现。

---

## 二、从 Topo_opt 构建非互斥软重叠超图

核心思路：将"找到第一个边缘节点就 break"改为"收集所有可达边缘节点并计算归属权重"。

### 2.1 新建：`C2E_Mapping_soft.m`

```matlab
function c2esoft = C2E_Mapping_soft(GF, EdgeSet, clients, ...
                                     virtual_src, virtual_dst, method)
% 返回 soft_assignment：num_clients × num_edges 的实值权重矩阵
% 每行可以有多个非零元素，行和为 1
%
% method 选项：
%   'path_all_edges'     - 路径上所有边缘节点等权分配
%   'flow_fractional'    - 逐 client-edge 对求 maxflow 容量（推荐）
%   'inverse_distance'   - 按网络距离倒数加权
%   'bandwidth_weighted' - 按可用带宽加权

c2esoft = zeros(length(clients), length(EdgeSet));

for i = 1:length(clients)
    s = clients(i);

    switch method
        case 'path_all_edges'
            % 方案A：找最短路径上所有边缘节点，等权
            P = shortestpath(GF, s, virtual_dst);
            if ~isempty(P)
                edges_on_path = intersect(P, EdgeSet);
                if ~isempty(edges_on_path)
                    idx = ismember(EdgeSet, edges_on_path);
                    c2esoft(i, idx) = 1 / length(edges_on_path);
                end
            end

        case 'flow_fractional'
            % 方案B（推荐）：逐 client-edge 对求 maxflow
            for j = 1:length(EdgeSet)
                e = EdgeSet(j);
                [mf, ~, ~, ~] = maxflow(GF, s, e);  % s → e 的最大流
                c2esoft(i, j) = mf;
            end
            total = sum(c2esoft(i, :));
            if total > 0
                c2esoft(i, :) = c2esoft(i, :) / total;
            end

        case 'inverse_distance'
            % 方案C：按网络距离倒数加权
            for j = 1:length(EdgeSet)
                e = EdgeSet(j);
                [~, d] = shortestpath(GF, s, e);
                if ~isinf(d) && d > 0
                    c2esoft(i, j) = 1 / d;
                end
            end
            total = sum(c2esoft(i, :));
            if total > 0
                c2esoft(i, :) = c2esoft(i, :) / total;
            end

        case 'bandwidth_weighted'
            % 方案D：沿路径推流，按可推流量加权
            % （需要记录各链路剩余带宽，复杂度较高，略）
            error('bandwidth_weighted not yet implemented');
    end
end
end
```

### 2.2 四种软分配方案的对比

| 方案 | 原理 | 优缺点 | 推荐度 |
|------|------|--------|--------|
| **A: path_all_edges** | 最短路径上所有边缘节点等权 | 简单但粗糙 | ⭐⭐ |
| **B: flow_fractional** | 对每对 (client, edge) 独立求 maxflow，按容量归一化 | 最贴合"软超图"数学含义，复用现有 maxflow 基础设施 | ⭐⭐⭐⭐⭐ |
| **C: inverse_distance** | 按跳数的倒数加权 | 计算量小但忽略带宽 | ⭐⭐ |
| **D: bandwidth_weighted** | 沿路径推流，按可推流量加权 | 最贴近实际但实现复杂 | ⭐⭐⭐ |

**推荐方案 B（flow_fractional）**：
- 直接复用现有 `maxflow()` 基础设施
- 权重天然反映"客户端 c 有多少通信容量能送达边缘节点 e"
- 数学含义：$H_{ce} = \frac{\text{capacity}(c \to e)}{\sum_{e'} \text{capacity}(c \to e')}$

### 2.3 输出形式

软超图输出一个 $N_\text{clients} \times N_\text{edges}$ 的**实值权重矩阵**：

```
        Edge1   Edge2   Edge3   Edge4   Edge5   Edge6
client1  0.70    0.30    0.00    0.00    0.00    0.00
client2  0.00    0.50    0.50    0.00    0.00    0.00
client3  0.00    0.20    0.30    0.50    0.00    0.00
  ...
client37 0.00    0.00    0.00    0.00    0.40    0.60
```

每行可以有多个非零元素，行和为 1。这就是软重叠超图的关联矩阵。

### 2.4 在 `HFL_SnF_v5.m` 中的集成

原来的 Phase 1（C2E）只需改一步：

```matlab
% 旧：硬分配
c2emap = C2E_Mapping_v4(GF, EdgeSet, ...);

% 新：软分配
c2esoft = C2E_Mapping_soft(GF, EdgeSet, ..., 'flow_fractional');
```

---

## 三、在 HFLSnF_KG_v3 中应用软重叠超图

### 3.1 改动清单

| 文件 | 改动内容 | 影响范围 |
|------|----------|----------|
| `core/topology.py` | `_normalize_groups` 允许重叠；`RoundTopology` 增加 `group_to_client_weights` | 数据结构 |
| `core/types.py` | `ClientUpdate` 增加 `group_weight` 字段 | 数据结构 |
| `core/aggregation.py` | `accumulate`/`finalize` 支持加权计数 | 核心聚合 |
| `fedml_kge/dynamic_trainer.py` | 训练循环按权重分片聚合 | 训练流程 |
| `core/matlab_topology_schedule.py` | 新增 `c2esoft` 矩阵解析 | 数据加载 |
| **新建** `core/soft_topology.py` | 纯 Python 软超图构建器（可选） | 独立性 |

### 3.2 修改 1：`core/topology.py` —— 允许重叠

```python
# 旧：互斥校验
def _normalize_groups(group_to_clients):
    all_clients = []
    for group_id, clients in sorted(group_to_clients.items()):
        all_clients.extend(clients)
    if len(set(all_clients)) != len(all_clients):
        raise ValueError("同一客户端不能同时出现在多个边缘组")

# 新：软重叠支持
def _normalize_groups_soft(group_to_clients_weights):
    """
    group_to_clients_weights: Dict[int, Dict[int, float]]
        组ID -> {客户端ID: 权重}
    允许客户端出现在多个组中，带权重。
    """
    for group_id, client_weights in group_to_clients_weights.items():
        for client_id, weight in client_weights.items():
            if not (0.0 < weight <= 1.0):
                raise ValueError(
                    f"权重须在 (0,1] 内: client {client_id} in group {group_id}"
                )
    return group_to_clients_weights
```

`RoundTopology` 数据类新增字段：

```python
@dataclass(frozen=True)
class RoundTopology:
    group_to_client_indexes: Dict[int, Tuple[int, ...]]       # 保留兼容
    group_to_client_weights: Dict[int, Dict[int, float]]      # ★ 新增：软权重
    active_client_indexes: Tuple[int, ...]
    edge_node_ids: Dict[int, int]
    participant_count: int
    source_round_index: int
```

### 3.3 修改 2：`core/types.py` —— `ClientUpdate` 增加权重

```python
@dataclass
class ClientUpdate:
    params: Dict[str, torch.Tensor]
    row_counts: Dict[str, torch.Tensor]     # 原有：行出现次数
    weight: float                           # 原有：三元组数量
    group_weight: float = 1.0               # ★ 新增：对此边缘组的软归属权重
    train_metrics: Optional[Dict] = None
```

### 3.4 修改 3：`core/aggregation.py` —— 核心聚合逻辑

这是最关键的改动。当前 `RowCountWeightedFedAvgAggregator`：

```python
# 现在：每个客户端在唯一的组中贡献 100%
def accumulate(self, updates):
    for update in updates:
        total_counts += update.row_counts                # 原样计数
        weighted_sum += update.params * update.row_counts

# 改为软权重：
def accumulate(self, updates):
    for update in updates:
        effective_counts = update.row_counts * update.group_weight  # ★
        total_counts += effective_counts
        weighted_sum += update.params * effective_counts
```

`finalize` 方法同理：归一化时使用加权后的总有效计数。

### 3.5 修改 4：`fedml_kge/dynamic_trainer.py` —— 训练循环

```python
# 原来：每个客户端只参与一个组
for group_id, group_clients in groups.items():
    group_updates = [updates[c] for c in group_clients]
    edge_stat = aggregator.accumulate(group_updates)

# 改为：按权重分片
for group_id, client_weights in groups.items():
    weighted_updates = []
    for client_id, weight in client_weights.items():
        update = deepcopy(updates[client_id])
        update.group_weight = weight           # 设置软权重
        weighted_updates.append(update)
    edge_stat = aggregator.accumulate(weighted_updates)
```

### 3.6 修改 5：`core/matlab_topology_schedule.py` —— 解析软矩阵

如果 MATLAB 侧输出 `c2esoft` 权重矩阵：

```python
def _read_soft_assignment(self, round_index):
    """从 .mat 文件中读取软分配矩阵"""
    soft_mat = self._data.get('c2esoft')
    if soft_mat is None:
        # 回退：从硬分配构造 one-hot 权重
        return self._hard_to_onehot(round_index)

    # soft_mat[round, :, :] 是 num_clients × num_edges 的矩阵
    round_mat = soft_mat[round_index, :, :]
    weights = {}
    for client_idx in range(round_mat.shape[0]):
        for edge_idx in range(round_mat.shape[1]):
            w = round_mat[client_idx, edge_idx]
            if w > 0:
                if edge_idx not in weights:
                    weights[edge_idx] = {}
                weights[edge_idx][client_idx] = float(w)
    return weights
```

### 3.7 新建：`core/soft_topology.py` —— 纯 Python 软超图构建

如果不想依赖 MATLAB，在 Python 端利用 networkx 自建：

```python
import numpy as np
import networkx as nx

class SoftHypergraphBuilder:
    """从网络拓扑直接构造软重叠超图"""

    def __init__(self, adj_matrix, edge_nodes, cloud_node):
        self.adj = adj_matrix        # N×N 邻接矩阵（含容量）
        self.edge_nodes = edge_nodes  # 候选边缘节点列表
        self.cloud = cloud_node       # 云节点ID
        self.G = self._build_graph()

    def _build_graph(self):
        G = nx.DiGraph()
        N = self.adj.shape[0]
        for i in range(N):
            for j in range(N):
                if self.adj[i, j] > 0:
                    G.add_edge(i, j, capacity=float(self.adj[i, j]))
        return G

    def build(self, clients, method='flow_fractional'):
        """对每个客户端计算对各边缘组的归属权重"""
        n_clients = len(clients)
        n_edges = len(self.edge_nodes)
        soft_H = np.zeros((n_clients, n_edges))

        for i, client in enumerate(clients):
            capacities = []
            for edge in self.edge_nodes:
                if method == 'flow_fractional':
                    try:
                        flow_value, _ = nx.maximum_flow(
                            self.G, int(client), int(edge)
                        )
                        capacities.append(flow_value)
                    except nx.NetworkXError:
                        capacities.append(0.0)
                elif method == 'inverse_distance':
                    try:
                        d = nx.shortest_path_length(
                            self.G, int(client), int(edge)
                        )
                        capacities.append(1.0 / max(d, 1))
                    except nx.NetworkXNoPath:
                        capacities.append(0.0)

            total = sum(capacities)
            if total > 0:
                soft_H[i, :] = [c / total for c in capacities]

        return soft_H
```

---

## 四、聚合流程完整改造：硬分配 vs 软重叠

```
硬分配聚合：
  client_1 ──→ Edge_1 ──→ Cloud  （全量）
  client_2 ──→ Edge_2 ──→ Cloud  （全量）
  client_3 ──→ Edge_1 ──→ Cloud  （全量）

软重叠聚合：
  client_1 ──→ Edge_1 (0.7) ──┐
             └─→ Edge_2 (0.3) ─┤──→ Cloud
  client_2 ──→ Edge_1 (0.4) ──┤  （加权合并后等价于
             └─→ Edge_2 (0.6) ─┘   每个 client 贡献 1.0）
```

关键不变式：每行权重和为 1，保证 `merge` 后总体信息量守恒。

---

## 五、潜在问题与应对

| 问题 | 影响 | 应对 |
|------|------|------|
| 参数被多组聚合后合并导致"双重计入" | 某 client 被过度计数 | 行和=1 保证总量守恒，merge 时各分片合并后恰为完整贡献 |
| maxflow 逐对求解开销 | 每轮额外跑 37×6=222 次 maxflow | MATLAB 的 maxflow 很快（毫秒级），且拓扑生成阶段只需跑一次 |
| 训练计算量增加 | 客户端需要多次前向/反向 | **不增加**：客户端本地只训练一次，分割仅发生在聚合阶段 |
| FedAdam 动量被多组更新 | 动量累积偏大 | finalize 后再运行 FedAdam，此时已是全局视角，不受影响 |
| 实验可比性 | 软超图参与方式与硬分组不同 | 保证总有效参与量（权重和）一致，如 HFL 35 个有效权重 ≈ FL 35 人 |

---

## 六、实现路线图

```
阶段 1：软超图构建（MATLAB 侧）
  □ 新建 C2E_Mapping_soft.m（方案 B: flow_fractional）
  □ 修改 HFL_SnF_v5.m / v6.m，集成软权重矩阵
  □ 输出 c2esoft 矩阵到 .mat 文件

阶段 2：Python 聚合层改造
  □ core/topology.py：RoundTopology 增加 group_to_client_weights
  □ core/types.py：ClientUpdate 增加 group_weight
  □ core/aggregation.py：accumulate / finalize 支持软权重
  □ 新建 core/soft_topology.py：纯 Python 构建器（可选）

阶段 3：训练器集成
  □ fedml_kge/dynamic_trainer.py：训练循环按权重分片聚合
  □ core/matlab_topology_schedule.py：解析 c2esoft 矩阵

阶段 4：实验验证
  □ 单元测试：聚合数学正确性（加权求和 = 全量贡献）
  □ smoke test：5 实体、2 关系小规模端到端
  □ formal run：与原硬分配对比 MRR / Hits@K / 收敛速度
```

---

## 七、核心总结

| 维度 | 硬超图（当前） | 软重叠超图（目标） |
|------|---------------|-------------------|
| **关联矩阵 H** | 每行恰好一个 1 | 每行多个非零值，行和为 1 |
| **客户端归属** | 唯一边缘组 | 多个边缘组的加权归属 |
| **聚合方式** | 组内求和 → 组间求和 | 组内加权求和 → 组间求和 → 总权重归一化 |
| **物理含义** | "客户端 c 必须经过边缘 e" | "客户端 c 的通信容量分配给了边缘 e₁, e₂…" |
| **MATLAB 来源** | C2E_Mapping (break) | C2E_Mapping_soft (flow_fractional) |
| **瓶颈** | 单路径单归属 | maxflow 逐对求解增加 K 倍开销 |

**一句话总结**："非互斥软重叠超图"的数学本质是把关联矩阵从 0-1 二值矩阵变为 [0,1] 实值矩阵。在 Topo_opt 侧利用现有 maxflow 逐对求容量归一化；在 HFLSnF_KG_v3 侧将聚合器从简单求和改为加权求和——其余结构保持不变。
