common_args:
  training_type: "simulation"
  random_seed: 42

data_args:
  dataset: "fb15k-237"
  data_dir: "data/FB15k-237"
  partition_strategy: "balanced_head_entity"

model_args:
  model: "transe"
  embedding_dim: 256
  distance_norm: 1

train_args:
  # Step-1 baseline: all 37 clients and six edge groups stay fixed.
  federated_optimizer: "DynamicTopologyTransE"
  comparison_scenario: "V3-Fixed37-Fixed6-ResetAdam-Seed42-Screen80"
  client_num_in_total: 37
  client_num_per_round: 37
  topology_type: "static"
  topology_architecture: "hfl"
  topology_snf: false
  topology_edge_mode: "fixed"
  edge_num: 6
  aggregation_mode: "row_count_weighted"
  local_objective: "bidirectional_self_adversarial"
  fede_gamma: 9.0
  adversarial_temperature: 1.0
  expected_partition_hash: "8bcac64b705ec2db8721de6a36130625a460c11e0da46e2c22bd852ff015fb19"
  # This hash covers the fixed six-group topology over all 80 rounds.
  expected_topology_schedule_hash: "69b9d605d4b8fa7da0d1f1900a081966f35ec1ffa84ca3d96baf1047ad25fc9c"
  comm_round: 80
  epochs: 2
  batch_size: 1024
  # Adam is intentionally rebuilt for every client call in this baseline.
  client_optimizer: "adam"
  client_optimizer_state_mode: "reset"
  learning_rate: 0.00005
  lr: 0.00005
  margin: 1.0
  negative_sample_count: 256
  # Use the same monitoring protocol as the dynamic 40/300-round runs.
  eval_every: 10
  validation_max_triples: 4096
  validation_selection: "relation_stratified"
  final_validation_max_triples: 4096
  # The small test subset checks the pipeline and never selects the method.
  test_max_triples: 512
  evaluation_query_batch_size: 64
  evaluation_candidate_batch_size: 8192
  profile_training_timing: false
  centralized_reference_mrr: 0.3869668845438231

device_args:
  using_gpu: true
  require_cuda: true
  gpu_id: 0

comm_args:
  backend: "sp"

tracking_args:
  enable_wandb: false
  using_mlops: false
  run_name: "hflsnf_kg_v3_screen_fixed37_fixed6_reset_adam_seed42_80round_cuda"
  result_root: "results"
